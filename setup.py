from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A classifier for actually entry-level jobs',
    author='Actually Entry Level',
    license='MIT',
)
